# Database connection strings for vim-dadbod
# Replace <user>, <password>, <host>, <port>, <dbname>, <sid>, etc. with actual values
# Ensure this file is not tracked by version control (add to .gitignore)

# MySQL Connection
# Remote: DB_UI_PROD_MYSQL=mysql://<user>:<password>@remote:3306/<remote_dbname>
# Local:  DB_UI_DEV_MYSQL=mysql://<user>:<password>@localhost:3306/<dbname>

# PostgreSQL Connection
# Local: DB_UI_DEV_POSTGRES=postgresql://<user>:<password>@localhost:5432/<dbname>
# Cloud: DB_UI_PROD_POSTGRES=postgresql://<user>:<password>@db.example.com:5432/<prod_dbname>

# SQLite Connection
# DB_UI_DEV_SQLITE=sqlite:/path/to/database.db

# Oracle / PL/SQL Connection
# Example: DB_UI_DEV_PLSQL=oracle://dev_user:dev_pass@localhost:1521/orcl
# DB_UI_DEV_PLSQL=oracle://<user>:<password>@<host>:<port>/<sid>

# MSSQL (note: on Arch, use Docker or install mssql-tools as needed)
# DB_UI_DEV_MSSQL=mssql://sa:Your_password123@localhost:1433/dev_db

# Local folder where you keep plugin checkouts
# LAZY_DEV_PATH=<path>

# Comma-separated list of plugin name patterns to treat as "dev"
# Example: "mavantgarderc,another-prefix"
# LAZY_DEV_PATTERNS=<gh_username>

# Whether to fall back to normal (non-dev) plugins when a dev version is missing
# "false"  -> only use plugins from LAZY_DEV_PATH that match patterns
# "true"   -> use dev plugin if present, otherwise use normal plugin
# LAZY_DEV_FALLBACK=false
